# Session log

Append-only, newest first. One entry per real training session: what was run,
what was observed, where each finding went. This is product evidence from use
— the *lessons that generalise* go to [DECISIONS.md](DECISIONS.md), and the
cadence (PLAN §F0) reads this file: the next user-visible merge waits on the
latest entry here.

---

## 2026-08-25 — Session 1 · solo · generated strength · current state (post-R6b)

First real session. Six findings, owner's words in spirit:

1. **"Kicks off without any context of what today's targets are, what we're
   doing — not a welcoming start."** → R6c's deferred claim now has its
   evidence date. Stays with **R6c** (built after the fix batch, once type is
   readable), shaped by the persona's opening ritual: *name the day and its
   shape, then start — no pep talk.*
2. **"Text too small on all screens — I have to come close to see what any
   exercise says."** → **fix batch.** Type on session screens must read at
   training distance, meters from the laptop. Walk criterion: attached frames
   must be legible at one-third scale.
3. **"Rest screens are dead."** — despite R6a shipping a rest screen "worth
   reading" nine days earlier. Cold start: the screen's one earned fact
   (`lastTime`) cannot exist in a first session. → **fix batch:** first-time
   content for cold stores ("First time on this one — we'll remember today
   for next time"), so the sessions that most need to impress aren't the
   emptiest.
4. **"Bent-over rows — nothing about speed of movement, here or anywhere. If
   it's my first time I don't know how to do it right."** → **fix batch,
   as content:** an authored per-exercise tempo cue (NOT `secondsPerRep`,
   which is the estimator's number), displayed on the work screen.
5. **"'Strength B done! Up next: Strength C' — I didn't get what B and C
   are."** → **fix batch, scoped:** the gate's heading prints the internal
   `block.label`; the movements are already listed by the next-up preview, so
   this is a label fix (position + content-derived words), not generator
   surgery.
6. **"The stretches seem unrelated to the muscles used in the workout."** →
   **F0.1's first input** — a cool-down selection rule keyed to
   `mobility.regions` (not `primaryMuscles`, which files Child's Pose under
   "core"), shaped as the corpus found it: a fixed, recognisable core chain
   plus a targeted prelude for what the day worked.

**Ruling recorded with this session:** solo iterations continue until these
six are fixed — a **closed list**; later sessions' findings feed later
batches but do not grow the duo bar unless the owner explicitly adds to it.
The duo session remains F0's exit, saved for the harder judge.
